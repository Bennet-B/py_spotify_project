"""FastAPI web layer for the Phase 2 workbench UI.

Depends on the framework-free core modules (client, cache, models, analyzer, insights); nothing in the core may import from here — enforced by ``tests/test_architecture.py``.
"""
